// import path from 'path';

// import cmytment from '../';
// import fs from 'fs-extra';

it('logs out the cwd', async () => {
  // const config: Config = {
  //   branch: 'main',
  //   repositories: [],
  //   rootDir: path.resolve(__dirname, 'sandbox', 'project'),
  // };
  // await cmytment(config);
});
